import { StyleSheet } from 'react-native'
import { ColorTheme } from '../../../common/AppStyles'

const styles = StyleSheet.create({
  mainContainer: {
    backgroundColor: ColorTheme.BACKGROUND_COLOR_01,
    height: '100%'
  }
})
export default styles
